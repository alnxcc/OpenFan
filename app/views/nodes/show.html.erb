<%=h @node.title%> <%= link_to 'New topic', new_node_topic_path(@node)%>

<% @node.topics.each do |topic|%>
	<div>Topic: <%= topic.title %> by <%= topic.user_id %><div>
	<div>Body: <%= topic.body %>
<%end%>
<%= link_to 'New topic', new_node_topic_path(@node)%>